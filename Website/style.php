:root {
    --trade-green: #00ff00;
    --trade-red: #ff0000;
    --trade-blue: #2563eb;
    --bg-light: #ffffff;
    --border: #e2e8f0;
}
body { background: var(--bg-light); font-family: 'Inter', sans-serif; color: #1e293b; }
.card { border-radius: 12px; border: 1px solid var(--border); box-shadow: 0 4px 6px -1px rgba(0,0,0,0.05); }
.logo-font { font-weight: 900; letter-spacing: -1px; }
.btn-trade { background: linear-gradient(90deg, var(--trade-green) 0%, var(--trade-red) 100%); color: white; border: none; font-weight: bold; }